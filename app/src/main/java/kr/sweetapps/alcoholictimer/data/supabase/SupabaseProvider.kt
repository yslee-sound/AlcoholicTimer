package kr.sweetapps.alcoholictimer.data.supabase

import android.content.Context

/** SupabaseProvider stub: returns null or throws if actual client required. */
object SupabaseProvider {
    fun getClient(context: Context): Any? = null
    fun reset() {}
}
