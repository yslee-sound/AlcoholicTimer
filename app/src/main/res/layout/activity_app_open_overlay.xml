<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- Transparent overlay that simply hosts the ad. No top bar or continue button shown by default. -->
    <FrameLayout
        android:id="@+id/overlay_content_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Optional continue button kept invisible by default to avoid duplicating ad UI.
         If needed, set visibility to visible in a variant layout to enable the top button. -->
    <Button
        android:id="@+id/btn_continue_app"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_gravity="top|end"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="12dp"
        android:backgroundTint="@android:color/transparent"
        android:text="" />

</FrameLayout>
