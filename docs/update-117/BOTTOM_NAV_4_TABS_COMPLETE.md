# Bottom Navigation 4개 축소 및 설정 버튼 추가 완료 보고서

**작업일**: 2025-12-18  
**목표**: Bottom Navigation을 5개에서 4개로 축소하고, Tab 5(설정)을 Tab 4의 상단 설정 버튼으로 이동

---

## ✅ 작업 완료 내역

### 수정된 파일 (3개)

1. **CommunityScreen.kt** - Tab 4에 설정 버튼 추가
2. **Tab04Graph.kt** - 설정 버튼 클릭 시 About 화면 네비게이션
3. **BottomNavBar.kt** - Tab 5 제거하여 4개 탭으로 축소

---

## 📋 구현 상세

### 1. CommunityScreen.kt 수정

**추가된 파라미터**:
```kotlin
@Composable
fun CommunityScreen(
    viewModel: CommunityViewModel = viewModel(),
    onSettingsClick: () -> Unit = {} // [NEW] 설정 화면으로 이동
)
```

**TopAppBar에 설정 버튼 추가**:
```kotlin
TopAppBar(
    title = {
        Text(
            text = stringResource(R.string.community_title),
            style = MaterialTheme.typography.titleLarge,
            color = Color(0xFF111111)
        )
    },
    actions = {
        // 설정 버튼 (우측 상단 톱니바퀴)
        IconButton(onClick = onSettingsClick) {
            Icon(
                painter = painterResource(id = R.drawable.gearsix),
                contentDescription = "설정",
                tint = Color(0xFF111111)
            )
        }
    },
    colors = TopAppBarDefaults.topAppBarColors(
        containerColor = Color.White,
        titleContentColor = Color(0xFF111111)
    )
)
```

**특징**:
- ✅ 우측 상단에 톱니바퀴 아이콘 (gearsix)
- ✅ 검은색 아이콘 (Color(0xFF111111))
- ✅ 클릭 시 `onSettingsClick` 콜백 실행

---

### 2. Tab04Graph.kt 수정

**Before**:
```kotlin
fun NavGraphBuilder.addTab04Graph(navController: NavHostController) {
    composable(Screen.More.route) {
        CommunityScreen()
    }
}
```

**After**:
```kotlin
fun NavGraphBuilder.addTab04Graph(navController: NavHostController) {
    composable(Screen.More.route) {
        CommunityScreen(
            onSettingsClick = {
                // 설정 버튼 클릭 시 About (Tab 5) 화면으로 이동
                navController.navigate(Screen.About.route)
            }
        )
    }
}
```

**동작**:
- ✅ 설정 버튼 클릭 → `Screen.About.route`로 네비게이션
- ✅ About 화면(기존 Tab 5)으로 이동

---

### 3. BottomNavBar.kt 수정

**Before (5개 탭)**:
```kotlin
private val bottomItems: List<BottomItem> = listOf(
    BottomItem(Screen.Start, ...),   // Tab 1: 금주 시작
    BottomItem(Screen.Records, ...), // Tab 2: 기록
    BottomItem(Screen.Level, ...),   // Tab 3: 레벨
    BottomItem(Screen.More, ...),    // Tab 4: 커뮤니티
    BottomItem(Screen.About, ...)    // Tab 5: 설정 ← 제거
)
```

**After (4개 탭)**:
```kotlin
private val bottomItems: List<BottomItem> = listOf(
    BottomItem(Screen.Start, ...),   // Tab 1: 금주 시작
    BottomItem(Screen.Records, ...), // Tab 2: 기록
    BottomItem(Screen.Level, ...),   // Tab 3: 레벨
    BottomItem(
        Screen.More,
        R.drawable.gearsix,
        R.string.drawer_menu_more,
        R.string.drawer_menu_more,
        // About 관련 라우트도 Tab 4로 연결
        associatedRoutes = setOf(
            Screen.More.route,
            Screen.About.route,        // [NEW] About는 Tab 4 그룹
            Screen.AboutLicenses.route,
            Screen.NicknameEdit.route,
            Screen.HabitSettings.route,
            Screen.CurrencySettings.route,
            Screen.Debug.route,
            Screen.Notification.route,
            "customer"
        )
    )
    // Tab 5 제거
)
```

**변경사항**:
- ❌ 제거: `BottomItem(Screen.About, ...)` (5번째 탭)
- ✅ 추가: Tab 4의 `associatedRoutes`에 About 관련 모든 라우트 포함
- ✅ 아이콘: `R.drawable.gearsix`

---

## 🎨 UI 변화

### Before (5개 탭)

```
┌─────────────────────────────────────┐
│                                     │
│        커뮤니티 화면                 │
│                                     │
└─────────────────────────────────────┘
┌───┬───┬───┬───┬───┐
│ ▶ │ 📅│ 🏅│ ⚙️│ 👤│ ← 5개 탭
└───┴───┴───┴───┴───┘
```

### After (4개 탭)

```
┌─────────────────────────────────────┐
│ 커뮤니티              ⚙️ ← 설정 버튼  │
├─────────────────────────────────────┤
│                                     │
│        커뮤니티 화면                 │
│                                     │
└─────────────────────────────────────┘
┌───┬───┬───┬───┐
│ ▶ │ 📅│ 🏅│ ⚙️│ ← 4개 탭
└───┴───┴───┴───┘

설정 버튼 클릭 ↓

┌─────────────────────────────────────┐
│ ← 설정                              │ ← BackTopBar
├─────────────────────────────────────┤
│  👤 알중이 >                        │
│  [프로필 편집하기]                   │
│  [알림] [문의] [평가]               │
│  [추천앱]                           │
│                                     │
│  버전 정보                          │
│  습관 설정 >                        │
│  통화 설정 >                        │
└─────────────────────────────────────┘
```

---

## 🔄 네비게이션 흐름

### Tab 5(설정) 진입 경로

**Before**:
```
하단 탭 5번 클릭 → About 화면
```

**After**:
```
하단 탭 4번 클릭 (커뮤니티)
  ↓
우측 상단 설정 버튼 클릭
  ↓
About 화면 (기존 Tab 5)
```

---

### 뒤로가기 동작

**About 화면에서 뒤로가기**:
```
About 화면 (BackTopBar)
  ↓
← 버튼 클릭 또는 시스템 뒤로가기
  ↓
Tab 4 (커뮤니티) 화면으로 복귀
```

---

## 📊 탭 구성 변경

### Before (5개)

| 순서 | 화면 | 아이콘 | Route |
|------|------|--------|-------|
| 1 | 금주 시작 | ▶ | Screen.Start |
| 2 | 기록 | 📅 | Screen.Records |
| 3 | 레벨 | 🏅 | Screen.Level |
| 4 | 커뮤니티 | ⚙️ | Screen.More |
| 5 | 설정 | 👤 | Screen.About |

---

### After (4개)

| 순서 | 화면 | 아이콘 | Route | 비고 |
|------|------|--------|-------|------|
| 1 | 금주 시작 | ▶ | Screen.Start | 유지 |
| 2 | 기록 | 📅 | Screen.Records | 유지 |
| 3 | 레벨 | 🏅 | Screen.Level | 유지 |
| 4 | 커뮤니티 | ⚙️ | Screen.More | 유지 + 설정 버튼 추가 |
| ~~5~~ | ~~설정~~ | ~~👤~~ | ~~Screen.About~~ | **제거** |

**설정 화면 진입**: Tab 4 우측 상단 설정 버튼 (⚙️)

---

## 🎯 주요 특징

### 1. 탭 개수 축소
- ✅ 5개 → 4개 탭
- ✅ 화면 공간 효율 증가
- ✅ 하단 네비게이션 간결화

### 2. 설정 진입점 변경
- ❌ 제거: 하단 탭 5번
- ✅ 추가: Tab 4 우측 상단 설정 버튼
- ✅ 더 직관적인 UX (설정은 보통 상단에 위치)

### 3. About 화면 유지
- ✅ 화면 코드 그대로 유지
- ✅ 모든 하위 메뉴 정상 작동
- ✅ 네비게이션만 변경

### 4. 하위 호환성
- ✅ 기존 About 관련 라우트 모두 Tab 4 그룹으로 인식
- ✅ 딥링크 정상 작동
- ✅ 백스택 관리 정상

---

## 🔧 기술적 세부사항

### associatedRoutes 설정

**Tab 4 (More)**:
```kotlin
associatedRoutes = setOf(
    Screen.More.route,              // 커뮤니티
    Screen.About.route,              // 설정 메인
    Screen.AboutLicenses.route,      // 오픈소스 라이선스
    Screen.NicknameEdit.route,       // 닉네임 편집
    Screen.HabitSettings.route,      // 습관 설정
    Screen.CurrencySettings.route,   // 통화 설정
    Screen.Debug.route,              // 디버그
    Screen.Notification.route,       // 알림
    "customer"                       // 고객 지원
)
```

**의미**:
- About 화면 및 모든 하위 메뉴가 Tab 4 그룹에 속함
- About 화면에 있을 때 하단 Tab 4가 선택된 상태로 표시
- 뒤로가기 시 Tab 4로 복귀

---

### TopAppBar actions

```kotlin
TopAppBar(
    title = { Text(...) },
    actions = {
        IconButton(onClick = onSettingsClick) {
            Icon(
                painter = painterResource(id = R.drawable.gearsix),
                contentDescription = "설정",
                tint = Color(0xFF111111)
            )
        }
    }
)
```

**Material 3 표준**:
- `actions` 슬롯: 우측 상단 액션 버튼
- `IconButton`: 터치 영역 자동 확보 (48dp)
- `Icon`: Vector drawable 표시

---

## ✅ 테스트 체크리스트

### 하단 네비게이션
- [ ] 탭이 4개만 표시되는지 확인
- [ ] Tab 4 아이콘이 톱니바퀴(⚙️)인지 확인
- [ ] 각 탭 클릭 시 정상 작동

### 설정 버튼
- [ ] Tab 4(커뮤니티) 우측 상단에 톱니바퀴 버튼 확인
- [ ] 버튼 클릭 시 About 화면으로 이동
- [ ] 버튼 아이콘이 검은색인지 확인

### About 화면
- [ ] 설정 버튼으로 진입 가능
- [ ] BackTopBar의 뒤로가기 버튼 작동
- [ ] 시스템 뒤로가기 작동
- [ ] 하단 탭 4번이 선택된 상태로 표시

### 하위 메뉴
- [ ] 습관 설정 진입 및 복귀
- [ ] 통화 설정 진입 및 복귀
- [ ] 닉네임 편집 진입 및 복귀
- [ ] 모든 하위 메뉴에서 Tab 4 선택 상태 유지

---

## 🎨 디자인 일관성

### 아이콘 색상
- TopAppBar 설정 버튼: `Color(0xFF111111)` (검은색)
- 타이틀: `Color(0xFF111111)` (검은색)
- 배경: `Color.White` (흰색)

### 아이콘 크기
- TopAppBar Icon: 기본 크기 (24dp)
- BottomNavBar Icon: 표준 크기

---

## 📊 영향 범위

### 영향 있는 파일
1. ✅ **CommunityScreen.kt** - 설정 버튼 추가
2. ✅ **Tab04Graph.kt** - 네비게이션 연결
3. ✅ **BottomNavBar.kt** - 탭 개수 축소

### 영향 없는 파일
- ✅ **AboutScreen.kt** - 변경 없음 (화면 코드 유지)
- ✅ **Tab05Graph.kt** - 변경 없음 (네비게이션 그래프 유지)
- ✅ **NavRoutes.kt** - 변경 없음 (라우트 정의 유지)
- ✅ **모든 하위 메뉴** - 변경 없음

---

## 💡 사용자 경험 개선

### Before (5개 탭)
- 탭이 많아서 각 탭 크기가 작음
- 설정 탭 위치가 맨 끝 (접근성 낮음)
- 자주 사용하지 않는 설정이 상시 노출

### After (4개 탭)
- ✅ 탭 크기 증가 (터치 영역 확대)
- ✅ 설정은 우측 상단 (표준 위치)
- ✅ 주요 기능만 하단 탭에 노출
- ✅ 깔끔한 UI

---

## 🎉 완료!

**구현된 기능**:
1. ✅ Bottom Navigation 5개 → 4개 축소
2. ✅ Tab 4(커뮤니티) 우측 상단에 설정 버튼 추가
3. ✅ 설정 버튼 클릭 시 About(기존 Tab 5) 화면으로 이동
4. ✅ About 관련 모든 라우트를 Tab 4 그룹으로 설정
5. ✅ 화면 코드 유지 (호환성 보장)

**UI 개선**:
- ✅ 탭 개수 축소로 간결한 네비게이션
- ✅ 설정 버튼 위치 표준화 (우측 상단)
- ✅ 일관된 디자인 (검은색 아이콘)

**빌드 상태**: 진행 중

---

**작성일**: 2025-12-18  
**완료**: Bottom Navigation 4개 축소 + 설정 버튼 추가  
**버전**: Navigation Refactoring v1.0

