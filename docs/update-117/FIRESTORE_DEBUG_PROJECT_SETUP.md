# 🔥 Firestore 권한 설정 (Debug 프로젝트)

## 📌 중요: Debug 프로젝트 사용 중

현재 디버그 빌드는 다음 Firebase 프로젝트를 사용합니다:

- **프로젝트 ID**: `sweetapps-dev-a3a03`
- **프로젝트 이름**: SweetApps Dev (개발/테스트용)
- **패키지명**: `kr.sweetapps.alcoholictimer.debug`

⚠️ **반드시 이 프로젝트의 Firestore 규칙을 수정해야 합니다!**

---

## ✅ 즉시 해결 방법

### 1️⃣ Firebase Console 접속

1. https://console.firebase.google.com/ 접속
2. **좌측 프로젝트 선택 드롭다운** 클릭
3. ⭐ **`sweetapps-dev-a3a03`** (SweetApps Dev) 선택
4. 좌측 메뉴: **Firestore Database** 클릭
5. 상단 탭: **규칙(Rules)** 클릭

---

### 2️⃣ Security Rules 수정

**기존 코드를 다음으로 교체:**

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // [Phase 2 테스트용] posts 컬렉션: 모든 읽기/쓰기 허용
    match /posts/{postId} {
      allow read, write: if true;
    }
    
    // 기존 컬렉션들 (records, diaries 등)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

---

### 3️⃣ 규칙 게시

1. **우측 상단 "게시(Publish)" 버튼** 클릭
2. 확인 팝업: **"게시"** 클릭
3. "규칙이 게시되었습니다" 메시지 확인

---

### 4️⃣ 앱에서 재테스트

1. 앱 실행 (디버그 빌드)
2. **Tab 5** → **Debug** 메뉴
3. 하단 **"커뮤니티 테스트"** 섹션으로 스크롤
4. **"📝 테스트 게시글 10개 생성"** 버튼 클릭
5. ✅ Toast: **"테스트 게시글 10개 생성 완료!"**
6. **Tab 4** 이동 → 게시글 10개 확인

---

## 🔄 Release 빌드 준비 시

Release 빌드는 다른 Firebase 프로젝트를 사용합니다:
- Release 프로젝트에도 동일한 규칙 설정 필요
- `app/src/release/google-services.json`의 `project_id` 확인

---

## 📋 체크리스트

- [ ] Firebase Console 접속
- [ ] **`sweetapps-dev-a3a03`** 프로젝트 선택 확인
- [ ] Firestore Database → 규칙(Rules)
- [ ] `match /posts/{postId}` 코드 추가
- [ ] "게시(Publish)" 버튼 클릭
- [ ] 앱 재테스트 → 성공 확인

---

**작성일**: 2025-12-17  
**작성자**: GitHub Copilot

